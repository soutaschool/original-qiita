<div class="col-md-offset-3 col-md-6">
<h1 class="big-log">Qiitaにログイン</h1>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= f.text_field :name, placeholder: 'ユーザー名またはメールアドレス',
     aria_label: 'ユーザー名またはメールアドレス', class: 'col-md-12' %>



  <div class="field">
    <%= f.password_field :password, placeholder: 'パスワード',
     aria_label: 'パスワード', class: 'col-md-12' %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Qiitaにログイン" %>
  </div>
<% end %>
</div>
Qiita のアカウントを持っていない場合は<%= link_to '新規登録',new_user_registration_path %>から。
